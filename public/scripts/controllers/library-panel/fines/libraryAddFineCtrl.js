'use strict';
app.controller('LibraryAddFineCtrl', function() {

//app.controller('LibraryAddFineCtrl', function($scope, $http, $location, $anchorScroll, $filter, Fine, identity, notifier, LibFineResource) {

    // Add Fine
  /*  $scope.addFine = function(fine) {
        Fine.add(fine).then(function() {
            notifier.success('Fine added successfully!');
            
            $location.path('/library-panel/fines');
            
        }, function(reason){
            handleError(reason);
        });
    };*/


});

